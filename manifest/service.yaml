kind: Service
apiVersion: v1
metadata:
  name: transaction-app
  labels:
    app: transaction-app
spec:
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  selector:
    app: transaction-app
    